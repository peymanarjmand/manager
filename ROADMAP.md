# Life Manager — نقشه‌ی راه بازطراحی و بازنویسی

> سند زنده‌ی پروژه. مرجع رسمی «کجا هستیم» و «قدم بعدی چیست». **مرحله‌به‌مرحله به‌روزرسانی می‌شود.**

- آخرین به‌روزرسانی: 2026-06-14
- مالک: پیمان · زبان اپ: فارسی/RTL · ۹۹٪ استفاده روی موبایل
- وضعیت‌ها: ✅ انجام‌شده · 🔄 در حال انجام · ⏳ برنامه‌ریزی‌شده · ⛔ بلاک‌شده

---

## 🎯 هدف
بازطراحی کامل و حرفه‌ای اپ مدیریت زندگی (با محوریت **حسابدار هوشمند**) به یک اپ موبایلِ مدرن، سریع، پایدار و امن — بدون از دست رفتن دیتا و بدون قطع شدن هیچ قابلیتی.

## ⛔ قیدهای سخت (نقض‌ناپذیر)
1. **هیچ دیتایی حذف نشود.**
2. **هیچ قابلیتی از کار نیفتد.**
3. **صفر هزینه** — فقط روی تیر رایگان Supabase + Netlify. (مثلاً Supabase branch پولی است؛ استفاده نمی‌کنیم.)
4. موبایل‌محور؛ حسابدار هوشمند بالاترین اولویت.

## 🛡️ گاردریل‌های ثابت (هر فاز)
- بکاپ کامل قبل از هر تغییر + اسکریپت بازگردانی آماده.
- تغییرات دیتابیس با شبیه‌سازی نقش کاربر (قبل/بعد) و مقایسه‌ی `count(*)` اعتبارسنجی شوند.
- تست‌های مشخصه برای ریاضیات پولی قبل از دست‌زدن به منطق حسابدار.
- چک‌لیست رفتاری بعد از هر تغییر دستی تست شود.
- مهاجرت‌ها فقط «insert اگر نبود» — هرگز ردیف موجود را بازنویسی نکن.
- انتشار مرحله‌ای و تأیید روی موبایل.

## 🎨 تصمیم‌های طراحی (قفل‌شده)
- رنگ برند: بنفش-نیلی `#6D5EF6` / `#8B7CFF` · درآمد `#34D399` · هزینه `#FB7185` · هشدار/چک `#FBBF24` · اطلاعات `#38BDF8`.
- تم: روشن + تیره با دکمه‌ی تغییر · فونت: وزیرمتن (ارقام هم‌عرض).
- ناوبری: Bottom-Nav + دکمه‌ی شناور «+» · AppBar با تاریخ شمسی + سوییچ تم + تنظیمات.
- صدر صفحه‌ی اصلی: (۱) قسط بعدی + اقساط نزدیک، (۲) طلب/بدهی اشخاص، (۳) نوار موجودی ماه.
- ماژول‌های صفحه‌ی اصلی: حسابدار/چک/دارایی‌وطلا/کارهای روزانه؛ خودرو‑رمزها‑دفترتلفن‑سلامت در «بیشتر».

## 📊 واقعیت‌های تأییدشده‌ی دیتا (2026-06-14)
تعداد ردیف‌ها: transactions=158 · ledger_entries=170 · installment_payments=508 · installment_plans=26 · darfak_expenses=88 · social_insurance=14 · people=8 · checks=1 · monthly_funds=1 · assets=0.
همه‌ی ردیف‌ها متعلق به **یک کاربر** (۰ ردیف بدون مالک) → بستن دسترسی عمومی امن است.

---

## 🗺️ فازها

### فاز ۰ — انجماد و بکاپ ✅
- [x] اسنپ‌شات کامل داخل دیتابیس در اسکیمای `backup_20260614` (همه‌ی جدول‌ها).
- [x] اعتبارسنجی برابری تعداد ردیف‌های اسنپ‌شات با اصل (۱۵۸/۱۷۰/۵۰۸/۲۶/۸۸/۱۴/... مطابق).
- [x] اسکریپت بازگردانی RLS + README در `backups/20260614/`.
- [ ] (اختیاری، off-site) خروجی از Supabase Dashboard → Database → Backups.

### فاز ۱ — امنیت 🔄 (هسته انجام شد)
- [x] فهرست کامل سیاست‌های RLS فعلی (برای بازگردانی).
- [x] بررسی: همه‌ی جدول‌های دارای سیاستِ باز، سیاست per-user هم داشتند → افزودن لازم نشد، قفل‌شدن رخ نداد.
- [x] حذف ۳۲ سیاستِ «باز برای همه» (`... anon all`، `kv_* anon`، `monthly_funds_all_*`).
- [x] اعتبارسنجی نقش `authenticated`/`anon` (قبل/بعد) + advisor رسمی → همه‌ی هشدارهای «RLS Always True» پاک شد، بدون قطعی قابلیت.
- [ ] **(۱b، مؤخر و همراه با تغییر کد):** خصوصی‌کردن باکت `lm-images` + Signed URL در `lib/idb-images.ts` + سیاست‌های مالک‌محور (تا نمایش/آپلود تصویر و PDF نشکند).
- [ ] (دستی در داشبورد) فعال‌کردن Leaked Password Protection + گزینه‌ی MFA.

### فاز ۲ — کف پایداری ✅
- [x] ErrorBoundary سراسری دور `Suspense` (`components/ErrorBoundary.tsx`) + گارد خطای chunk بعد از دیپلوی.
- [x] نصب `@types/react`/`@types/react-dom` → typecheck حالا **واقعی** است و **۰ خطا**؛ ۱۰ خطای پنهان رفع شد (از جمله union ناقص `View` که حالا daily-tasks/assets/darfak را دارد).
- [x] سیم‌کشی `@typescript-eslint` + `no-floating-promises` (warn) + `exhaustive-deps` (warn) → ۸۷ هشدار سطح‌بندی شد (نقشه‌ی کار فاز ۳/۷، بدون شکست build).
- [x] Vitest + موتور محاسبات خالص `features/smart-accountant/calculations.ts` + ۱۴ تست مشخصه (مانده، اقساط، APR، فرمت‌ها) — همه سبز.
- [x] اعتبارسنجی کامل: `typecheck` ۰ خطا · `test` ۱۴/۱۴ · `build` موفق · `lint` بدون error.

### فاز ۳ — دوام نوشتن ✅ (هسته‌ی حسابدار)
- [x] زیربنای صف پایدار `lib/outbox.ts` (IndexedDB via `idb`) + ارسال مجدد روی online/focus/visibility + ۴ تست واحد.
- [x] وصل‌کردن همه‌ی اکشن‌های نوشتن store حسابدار (~۲۵ مورد) به صف، با حفظ آپدیت خوش‌بینانه؛ تست‌های مشخصه سبز.
- [x] `initOutbox()` در استارت (`index.tsx`) + نشانگر وضعیت همگام‌سازی `components/SyncIndicator.tsx`.
- [→] (۳b) نوشتن‌های `features/assets` و `features/my-car` **به فاز ۶ منتقل شد**: این store‌ها الگوی «نوشتن→reload» دارند و وصل‌کردن امن به صف نیاز به بازساخت به الگوی خوش‌بینانه دارد؛ همراه ریفکتور معماری انجام می‌شود. (دیتای فعلی‌شان دست‌نخورده و مثل قبل ذخیره می‌شود.)

### فاز ۴ — شناسه‌های امن ✅
- [x] کمک‌تابع `lib/id.ts` (`newId()` با `crypto.randomUUID()` و fallback).
- [x] جایگزینی شناسه‌سازی `Date.now()` در حسابدار (تراکنش/شخص/دفتر/چک/طرح اقساط/تأمین/درفک)، کارهای روزانه، دفترتلفن، رمزها. شناسه‌های قدیمی دست‌نخورده.
- [→] assets / my-car / health باقی مانده‌اند و همراه فازهای مربوط به خودشان انجام می‌شوند.

### فاز ۵ — هم‌گرایی دیتا ✅ (هسته)
- [x] کوئری تأیید: همه‌ی رکوردهای بلوب در جدول‌ها موجودند (`missing_in_table = 0` برای همه‌ی موجودیت‌ها).
- [x] `partialize` + `merge` امن در store: بلوب `state_accountant` فقط تنظیمات UI را نگه می‌دارد؛ دیتای مالی فقط از جدول‌ها (`load*()`) می‌آید — پایان منبع دوگانه‌ی حقیقت و آپلود سنگین هر بار.
- [→] تک‌بارکردن/حذف کد مهاجرت قدیمی + حذف فراخوانی بازگشتی `loadPeopleAndLedger`: الان بی‌اثر است (کلید رمز هرگز ست نمی‌شود → no-op)؛ پاک‌سازی همراه استخراج repository در فاز ۶.
- یادداشت دیپلوی: بعد از دیپلوی، بلوب با اولین نوشتن کوچک می‌شود؛ `merge` از تزریق دیتای کهنه‌ی بلوب قدیمی جلوگیری می‌کند.

### فاز ۶ — معماری ماژولار ⏳
- [ ] قالب واحد ماژول (ui/model/data/types/index).
- [ ] لایه‌ی data per-entity (ریپازیتوری) + حذف مپینگ‌های تکراری.
- [ ] شکستن فایل‌های غول‌پیکر (MyCar 2097، store 1032، OwnerGold 895) پشت همان API.
- [ ] (از ۳b) بازساخت store‌های assets و my-car به الگوی خوش‌بینانه + وصل‌کردن نوشتن‌هایشان به صف `lib/outbox.ts` (دوام آفلاین کامل).
- [ ] یک‌سان‌سازی تایپ‌ها + اصلاح `View` + مسیریابی Back/deep-link.

### فاز ۷ — سرعت 🔄
- [x] حذف وابستگی‌های بلااستفاده (jspdf، html2canvas، date-fns).
- [x] اصلاح باگ chunkبندی: `charts` از چانک eager react جدا شد؛ دیگر چانک pdf/canvas نیست؛ react ~۸KB سبک‌تر.
- [x] اسکلتون LCP در `index.html` (اسپینر تمیز به‌جای صفحه‌ی خالی هنگام بارگذاری).
- [ ] اشتراک انتخابی Zustand + `React.memo` (کاهش ری‌رندر کل درخت حسابدار) — بهتر است همراه تأیید رفتاری/دیپلوی.
- [ ] جایگزینی `jalali-moment` با گزینه‌ی سبک (۳۰+ نقطه‌ی استفاده؛ نیاز به تطبیق دقیق خروجی تاریخ).

### فاز ۸ — PWA حرفه‌ای 🔄
- [x] جایگزینی SW دستی با `vite-plugin-pwa`/Workbox: precache همه‌ی ۲۲ asset هش‌دار (رفع خرابی آفلاین بعد از دیپلوی) + navigateFallback به index.html.
- [x] آپدیت کنترل‌شده (`registerType: 'prompt'`) + بنر `components/PwaUpdatePrompt.tsx`؛ بدون swap ناگهانی چانک. (گارد خطای chunk قبلاً در ErrorBoundary اضافه شده بود.)
- [x] Supabase همچنان NetworkOnly (دیتای مالی کش نمی‌شود) · manifest از کانفیگ تولید و در HTML تزریق می‌شود · `vite-env.d.ts` برای تایپ‌ها.
- [ ] آیکون‌های PNG/maskable + apple-touch PNG + شورتکات‌ها — نیاز به تولید فایل PNG (پولیش باقی‌مانده).

### فاز ۹ — ردیزاین کامل اپ 🎨 🔄
- [x] دیزاین‌سیستم پایه: فونت **وزیرمتن** (self-host woff2، اعمال سراسری)، پالت برند بنفش‌نیلی + **remap اکسنت sky→ایندیگو** (کل اپ یک‌دست شد، بدون ویرایش فایل‌به‌فایل)، toolهای `nums-tabular`/شعاع/safe-area.
- [x] پوسته‌ی موبایل: `BottomNav` (با FAB مرکزی) + AppBar برندشده («مدیر زندگی») + safe-area + `viewport-fit=cover`.
- [x] بازطراحی صفحه‌ی اصلی (مالی‌محور): هیرو «قسط بعدی» + طلب/بدهی + گرید ماژول‌ها (+ دفترتلفن که قبلاً در دسترس نبود)؛ ساعت/پومودورو پایین‌تر. helper `getNextUnpaidInstallment` + تست.
- [x] **تم بصری جدید (مطابق ۴ تصویر مرجع کاربر):** پس‌زمینه‌ی سرمه‌ای با هاله‌ی بنفش، پرایمیتیوهای `ProgressRing`/`Sparkline`، آیکون‌های حرفه‌ای lucide در چیپ‌های تینت، Bottom-Nav با FAB درخشان، AppBar با آواتار/Online. صفحه‌ی اصلی کامل مطابق تصویر ۱ بازطراحی شد.
- [x] **حسابدار مطابق تصاویر مرجع:** خلاصه (حلقه+اسپارک‌لاین، تصویر ۲)، لیست اقساط (حلقه‌ی ماه + کارت‌های گلَسی، تصویر ۳)، جزئیات قسط (تایم‌لاین عمودی + حلقه، تصویر ۴). همه‌ی محاسبات/اکشن‌ها دست‌نخورده.
- [x] **یک‌دست‌سازی سراسری:** همه‌ی صفحات (بقیه‌ی تب‌های حسابدار + دارایی/خودرو/کارها/رمزها/دفترتلفن/سلامت/تنظیمات/ورود) به سطوح و حاشیه‌های گلَسیِ تم منتقل شدند (۳۹ فایل، فقط استایل).
- [ ] (پولیش اختیاری) افزودن اجزای خلاقانه‌ی اختصاصی (حلقه/آیکون/تایم‌لاین) به صفحات منتخب طبق نظر کاربر.
- [ ] Bottom-Sheet/Toast مشترک + تم روشن/تیره با سوییچ.
- [ ] Bottom-Sheet/Toast مشترک + تم روشن/تیره با سوییچ.

---

## 📍 موقعیت فعلی
**فاز ۰–۵، ۷a/LCP، ۸ ✅** و **فاز ۹ (ردیزاین) عملاً کامل**: کل اپ در تم سرمه‌ای/ایندیگو گلَسی با وزیرمتن و آیکون lucide؛ صفحه‌ی اصلی + ۴ صفحه‌ی مرجع حسابدار به‌صورت اختصاصی، بقیه‌ی صفحات یک‌دست؛ ورود/ثبت‌نام هم در تم. همه `typecheck`/`test`/`build` سبز، بدون از دست رفتن دیتا یا قطعی قابلیت.

**باقی‌مانده‌های اختیاری (نیازمند تکرار روی دستگاه):** بازطراحی عمیقِ اختصاصیِ ماژول‌های بزرگ (خودرو/دارایی/کارها)، سوییچ تم روشن/تیره (نیازمند توکن‌بندی)، Bottom-Sheet/Toast. و کارهای غیر‌ردیزاین: فاز ۶ معماری · ۳b دارایی/خودرو outbox · ۱b استوریج · تنظیمات Auth.

## 📝 تغییرات
- 2026-06-14: ساخت رودمپ. تصمیم‌های طراحی قفل شد. شروع فاز ۰/۱.
- 2026-06-14: فاز ۰ — اسنپ‌شات کامل `backup_20260614` + تأیید تعداد ردیف‌ها + اسکریپت بازگردانی RLS.
- 2026-06-14: فاز ۱ — حذف ۳۲ سیاست RLS باز؛ تأیید با شبیه‌سازی نقش `authenticated`/`anon` و advisor؛ نشت عمومی داده بسته شد، بدون از دست رفتن دیتا و بدون قطعی قابلیت.
- 2026-06-14: فاز ۲ — ErrorBoundary، نصب @types/react (typecheck واقعی، ۰ خطا)، سیم‌کشی ESLint (۸۷ هشدار)، Vitest + ۱۴ تست مشخصه. typecheck/test/build/lint سبز.
- 2026-06-14: فاز ۳ (هسته) — صف پایدار `lib/outbox.ts` + ۴ تست؛ وصل‌کردن ~۲۵ اکشن نوشتن حسابدار؛ initOutbox + SyncIndicator. هشدار promise: ۸۷→۶۵. typecheck/test(۱۸)/build سبز.
- 2026-06-14: فاز ۴ — `lib/id.ts` (`newId()` با crypto.randomUUID)؛ جایگزینی شناسه‌سازی Date.now در حسابدار/کارها/دفترتلفن/رمزها. typecheck/test/build سبز.
- 2026-06-14: فاز ۵ — راستی‌آزمایی بلوب↔جدول (۰ گمشده)؛ `partialize`+`merge` روی state_accountant (پایان منبع دوگانه). typecheck/test/build سبز.
- 2026-06-14: فاز ۷a — حذف jspdf/html2canvas/date-fns؛ اصلاح manualChunks (charts جدا از react). build سبز.
- 2026-06-14: فاز ۸ (هسته) — vite-plugin-pwa جایگزین SW دستی؛ precache ۲۲ asset؛ بنر آپدیت کنترل‌شده؛ Supabase NetworkOnly. typecheck/test/build سبز.
- 2026-06-14: فاز ۷ (LCP) — اسکلتون اسپینر در index.html.
- 2026-06-14: فاز ۹ (افزایش ۱) — وزیرمتن سراسری، برند ایندیگو (+remap sky)، BottomNav+AppBar، بازطراحی صفحه‌ی اصلی مالی‌محور. typecheck/test(۲۰)/build سبز.
- 2026-06-14: فاز ۹ (افزایش ۲) — تم بصری مرجع کاربر (پس‌زمینه‌ی سرمه‌ای+هاله، ProgressRing/Sparkline، آیکون lucide، FAB درخشان). هیروی صفحه‌ی اصلی = قسط بعدی + نوار پیشرفت اقساط ماه. پوسته‌ی حسابدار + کارت‌های خلاصه در تم جدید. فوتر: «توسط پیمان ارجمند».
- 2026-06-14: فاز ۹ (افزایش ۳) — حسابدار مطابق هر ۴ تصویر مرجع: خلاصه (حلقه+اسپارک‌لاین)، لیست اقساط (حلقه‌ی ماه + کارت گلَسی)، جزئیات قسط (تایم‌لاین عمودی). typecheck/test(۲۰)/build سبز.
- 2026-06-14: فاز ۹ (افزایش ۴) — یک‌دست‌سازی سراسری تم گلَسی روی ۳۹ فایل (همه‌ی صفحات و ماژول‌ها). typecheck/test/build سبز.
- 2026-06-14: فاز ۹ (افزایش ۵) — رفع عارضه‌ی کلاس‌های نامعتبر sweep؛ پولیش صفحات ورود/ثبت‌نام (لوگوی برند، پس‌زمینه‌ی گرادیانی).
