# راهنمای نصب و راه‌اندازی داشبورد سلامتی

## پکیج‌های مورد نیاز

برای اجرای کامل داشبورد سلامتی، باید پکیج‌های زیر را نصب کنید:

```bash
npm install idb chart.js react-chartjs-2 date-fns jspdf html2canvas lucide-react
```

## توضیح پکیج‌ها

- **idb**: برای کار با IndexedDB و ذخیره‌سازی آفلاین داده‌ها
- **chart.js & react-chartjs-2**: برای ایجاد نمودارهای پیشرفته و تعاملی
- **date-fns**: برای مدیریت تاریخ و زمان
- **jspdf & html2canvas**: برای تولید گزارش‌های PDF
- **lucide-react**: برای آیکون‌های مدرن (اختیاری - می‌توان از آیکون‌های موجود استفاده کرد)

## ساختار پروژه

```
features/health-dashboard/
├── HealthDashboard.tsx          # کامپوننت اصلی
├── components/
│   ├── HealthCards.tsx         # کارت‌های سلامت
│   ├── ChartContainer.tsx      # کانتینر نمودارها
│   ├── NotificationBadge.tsx    # نشان‌دهنده یادآوری‌ها
│   └── QuickActions.tsx        # اقدامات سریع
├── nutrition/
│   └── NutritionTracker.tsx    # ترکر تغذیه
├── services/
│   └── healthDataService.ts    # سرویس داده‌های سلامتی
├── store/
│   └── healthStore.ts          # استور Zustand
└── index.ts                    # فایل export
```

## ویژگی‌های پیاده‌سازی شده

### ✅ داشبورد اصلی سلامت
- نمایش کلی وضعیت سلامت
- کارت‌های سلامت برای وزن، فشار خون، تغذیه و داروها
- نمودارهای وزن و فشار خون
- اقدامات سریع برای دسترسی آسان

### ✅ ترکر تغذیه
- ثبت وعده‌های غذایی با جزئیات کامل
- محاسبه خودکار کالری، پروتئین، کربوهیدرات و چربی
- مشاهده خلاصه روزانه
- مدیریت وعده‌های غذایی (افزودن، ویرایش، حذف)

### ✅ سیستم ذخیره‌سازی آفلاین
- استفاده از IndexedDB برای ذخیره‌سازی داده‌ها
- بدون نیاز به اینترنت برای عملکرد اصلی
- قابلیت همگام‌سازی در آینده

### ✅ نمودارهای پیشرفته
- نمودار وزن با روند تغییرات
- نمودار فشار خون (سیستولیک و دیاستولیک)
- رنگ‌بندی بر اساس استانداردهای پزشکی
- نمایش میانگین و درصد تغییرات

## ویژگی‌های در دست توسعه

### 🔜 مدیریت داروها
- ثبت داروها با زمان‌بندی
- سیستم یادآوری هوشمند
- تاریخچه مصرف داروها

### 🔜 ترکر فشار خون
- ثبت اندازه‌گیری‌های روزانه
- تحلیل روند تغییرات
- هشدار برای مقادیر خطرناک

### 🔜 کنترل وزن
- ثبت وزن روزانه
- محاسبه BMI خودکار
- هدف‌گذاری کاهش/افزایش وزن

### 🔜 برنامه‌ریزی هفتگی
- تقویم سلامتی
- برنامه‌های ورزشی و غذایی
- یادآوری‌های خودکار

### 🔜 گزارش‌های پزشکی
- خروجی PDF با کیفیت بالا
- خروجی Excel برای تحلیل
- گزارش‌های دوره‌ای

## نحوه استفاده

1. **نصب پکیج‌ها**: دستور npm install بالا را اجرا کنید
2. **اجرا**: `npm run dev`
3. **دسترسی**: از داشبورد اصلی روی "داشبورد سلامتی" کلیک کنید

## تنظیمات اولیه

در اولین استفاده، سیستم به طور خودکار پایگاه داده IndexedDB را ایجاد می‌کند. برای تست می‌توانید:

1. چند وعده غذایی ثبت کنید
2. وزن و فشار خون خود را وارد کنید
3. داروهایتان را ثبت کنید
4. گزارش‌ها را مشاهده کنید

## مشکلات احتمالی

### ارور پکیج‌های نصب نشده
اگر با ارور مواجه شدید، مطمئن شوید که Node.js نسخه 20 یا بالاتر دارید:

```bash
node --version
```

### مشکل با Chart.js
اگر نمودارها به درستی نمایش داده نمی‌شوند، مطمئن شوید که chart.js و react-chartjs-2 به درستی نصب شده‌اند.

### مشکل با IndexedDB
در مرورگرهای قدیمی ممکن است IndexedDB پشتیبانی نشود. در این صورت باید از polyfill استفاده کنید.

## پشتیبانی

برای گزارش مشکلات یا پیشنهادات، لطفاً از بخش Issues پروژه استفاده کنید.